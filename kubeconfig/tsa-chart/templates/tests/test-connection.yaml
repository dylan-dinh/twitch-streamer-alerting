apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tsa-chart.fullname" . }}-test-connection"
  labels:
    {{- include "tsa-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tsa-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
