apiVersion: v1
kind: Service
metadata:
  name: {{ include "tsa-chart.fullname" . }}
  labels:
    {{- include "tsa-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tsa-chart.selectorLabels" . | nindent 4 }}
